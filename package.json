{
  "name": "vps-remaining-value",
  "version": "0.2.0",
  "description": "VPS Remaining Value Calculator - Cloudflare Pages Edition",
  "scripts": {
    "generate:logo": "node scripts/generate-logo.js",
    "predeploy": "npm run generate:logo",
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name=vps-remaining-value",
    "start:express": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.4.5",
    "cookie-parser": "^1.4.6"
  },
  "devDependencies": {
    "@yao-pkg/pkg": "^6.20.0",
    "wrangler": "^3.0.0"
  },
  "pkg": {
    "assets": [
      "public/**/*"
    ]
  }
}
